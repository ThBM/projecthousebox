{% extends "Entreprise/base.html.twig" %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1>Ajouter un nouveau projet</h1>
        </div>
        <div class="card-body">

            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit">
            {{ form_end(form) }}

        </div>
    </div>
{% endblock %}